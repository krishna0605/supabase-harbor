{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run typecheck"
  },
  "deploy": {
    "startCommand": "npm run worker:sweep",
    "cronSchedule": "*/15 * * * *",
    "restartPolicyType": "NEVER"
  }
}
